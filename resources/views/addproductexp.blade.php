<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tambah ProductExp</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="p-6">
    {{-- <h1 class="text-2xl font-bold mb-4">Tambah ProductExp</h1>

    @if ($errors->any())
        <div class="bg-red-200 text-red-700 p-3 mb-4 rounded">
            <ul>
                @foreach ($errors->all() as $error)
                    <li>{{ $error }}</li>
                @endforeach
            </ul>
        </div>
    @endif

    <form action="{{ route('productexp.store') }}" method="POST">
        @csrf
        <label class="block mb-2">Pilih Produk:</label>
        <select name="product_id" class="border p-2 w-full mb-4">
            @foreach ($products as $product)
                <option value="{{ $product->id }}">{{ $product->name }}</option>
            @endforeach
        </select>

        <label class="block mb-2">Quantity:</label>
        <input type="number" name="quantity" class="border p-2 w-full mb-4" required>

        <label class="block mb-2">Price Discount:</label>
        <input type="number" name="price_discount" class="border p-2 w-full mb-4" required>

        <label class="block mb-2">Expired Date:</label>
        <input type="date" name="expired_at" class="border p-2 w-full mb-4" required>

        <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded">Simpan</button>
        <a href="{{ route('productexp.index') }}" class="bg-gray-500 text-white px-4 py-2 rounded">Batal</a>
    </form> --}}

    ppp
</body>
</html>
